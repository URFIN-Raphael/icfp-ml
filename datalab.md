Go to : https://datalab.sspcloud.fr/my-services

In the service catalog select : Jupyter-pytorch-gpu

Once launched, in a terminal run:
`git clone https://github.com/dataflowr/notebooks.git`

`cd notebooks`

`pip install -r requirements.txt`

You are all set!